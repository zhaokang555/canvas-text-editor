enum CursorType {
  defaultCursor = 'default',
  ewResize = 'ew-resize',
  nsResize = 'ns-resize',
  neswResize = 'nesw-resize',
  nwseResize = 'nwse-resize',
  move = 'move',
  text = 'text',
}

export default CursorType;
